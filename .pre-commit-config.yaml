repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.22 # keep in step with the ruff the CI resolves, or local fixes diverge from CI
    hooks:
      - id: ruff-check
        args: [--fix]
      - id: ruff-format
